[runtime]
log_level = "INFO"

[normalize.data_writer]
# Disable automatic schema evolution so unexpected columns surface as errors
# rather than silently landing in the warehouse.
disable_compression = false
